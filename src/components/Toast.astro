---
const { message = '', type = 'info', show = false } = Astro.props;
---

<div
  id="toast"
  class:list={[
    'fixed bottom-6 right-6 z-50 px-4 py-2 rounded-lg shadow-lg text-white transition-opacity duration-300',
    type === 'success' ? 'bg-green-600' : '',
    type === 'error' ? 'bg-red-600' : '',
    type === 'info' ? 'bg-black' : '',
    show ? 'opacity-100' : 'opacity-0 pointer-events-none'
  ]}
>
  {message}
</div>
